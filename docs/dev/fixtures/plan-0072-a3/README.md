# Plan 0072 A3 redacted fixtures

These fixtures contain deterministic synthetic labels and bytes only. They
exercise voice-sample custody, soft anonymous clustering, exact reviewed
profile allowlists, and previewed deletion without representing a real voice,
person, recording, private filesystem path, or provider operation.

Private object identifiers remain test-local. Portable fixture and ledger
readbacks expose hashes and bounded lineage, never raw bytes or storage paths.
