# Plan 0072 A4 redacted fixtures

The supervisor replay uses synthetic selectors, identifiers, scores, factors,
and hashes. It contains no provider body, credential, private transcript,
audio, biometric payload, real tenant selector, or accepted identity effect.

The fixture proves exact source-scope binding, visible evidence pillars,
deterministic combined scoring, and a zero-effect contract-fixture run.
